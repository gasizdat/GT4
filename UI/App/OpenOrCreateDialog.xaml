<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="GT4.UI.OpenOrCreateDialog"
             xmlns:gt4_ui="clr-namespace:GT4.UI"
             Title="{Binding DialogTitle}">
  <ScrollView>

    <VerticalStackLayout Padding="20" Spacing="10">
      <Label 
            Text="{Binding DialogHint}"
            VerticalOptions="Center" 
            HorizontalOptions="Center" />

      <CollectionView ItemsSource="{Binding Projects}">
        <CollectionView.ItemTemplate>
          <DataTemplate x:DataType="gt4_ui:ProjectListItem">
            <VerticalStackLayout>
              <Label Text="{Binding Name}" FontSize="20" FontAttributes="Bold" Padding="10"/>
              <Label Text="{Binding Path}" FontSize="12" Padding="10"/>
            </VerticalStackLayout>
          </DataTemplate>
        </CollectionView.ItemTemplate>
      </CollectionView>

    </VerticalStackLayout>

  </ScrollView>

</ContentPage>