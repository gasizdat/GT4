<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="GT4.UI.Pages.SettingsPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:components="clr-namespace:GT4.UI.Components"
    xmlns:resources="clr-namespace:GT4.UI.Resources;assembly=GT4.UI.Resources"
    x:Name="Root"
    Title="{x:Static resources:UIStrings.TitleSettingsPage}">

  <components:PageLayout>
    <components:PageLayout.Body>
      <CollectionView ItemsSource="{Binding SettingEditors, Source={x:Reference Root}}">

        <CollectionView.ItemsLayout>
          <LinearItemsLayout
              ItemSpacing="{StaticResource PageContentSpacing}"
              Orientation="Vertical" />
        </CollectionView.ItemsLayout>

        <CollectionView.ItemTemplate>
          <DataTemplate>
            <Border>
              <components:SettingEditorView Editor="{Binding .}" />
            </Border>
          </DataTemplate>
        </CollectionView.ItemTemplate>
      </CollectionView>
    </components:PageLayout.Body>
  </components:PageLayout>

</ContentPage>
