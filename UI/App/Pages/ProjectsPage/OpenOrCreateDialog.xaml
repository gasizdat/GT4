<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="GT4.UI.App.Pages.OpenOrCreateDialog"
             xmlns:items="clr-namespace:GT4.UI.App.Items"
             xmlns:resources="clr-namespace:GT4.UI.Resources;assembly=GT4.UI.Resources"
             Title="{x:Static resources:UIStrings.WndTitleOpenOrCreateDialog}">
  <ScrollView>

    <VerticalStackLayout Padding="20" Spacing="10">
      <Label 
            Text="{x:Static resources:UIStrings.WndHintOpenOrCreateDialog}"
            VerticalOptions="Center" 
            HorizontalOptions="Center" />

      <CollectionView 
          ItemsSource="{Binding Projects}" 
          SelectionMode="Single" 
          SelectionChanged="OnProjectSelected">
        
        <CollectionView.ItemTemplate>
          <DataTemplate x:DataType="items:ProjectItem">

            <FlexLayout 
                Direction="Row"
                HeightRequest="50" >

              <Image 
                  Source="{Binding ProjectImage}" 
                  WidthRequest="32" 
                  HeightRequest="32" 
                  Margin="2"/>

              <VerticalStackLayout
                  FlexLayout.Grow="1">

                <Label 
                    Text="{Binding Name}" 
                    FontSize="20" 
                    FontAttributes="Bold" 
                    Padding="2"/>

                <Label 
                    Text="{Binding Description}" 
                    FontSize="12" 
                    Padding="2"/>
                
              </VerticalStackLayout>

              <Button 
                  Text="❌" 
                  Clicked="OnDeleteProjectSelected"
                  WidthRequest="32" 
                  HeightRequest="32"
                  Padding="0"
                  IsVisible="{Binding IsHandlesVisible}"/>

            </FlexLayout>
          </DataTemplate>
        </CollectionView.ItemTemplate>
      </CollectionView>

    </VerticalStackLayout>

  </ScrollView>

</ContentPage>