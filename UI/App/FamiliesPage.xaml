<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="GT4.UI.FamiliesPage"
             xmlns:ui="clr-namespace:GT4.UI"
             xmlns:resources="clr-namespace:GT4.UI.Resources;assembly=GT4.UI.Resources"
             Title="{x:Static resources:UIStrings.WndTitleFamiliesPage}">
  <ScrollView>

    <VerticalStackLayout Padding="20" Spacing="10">
      <Label 
            Text="{x:Static resources:UIStrings.WndHintFamiliesPage}"
            VerticalOptions="Center" 
            HorizontalOptions="Center" />

      <CollectionView 
          ItemsSource="{Binding Families}" 
          SelectionMode="Single" 
          SelectionChanged="OnFamilySelected">
        
        <CollectionView.ItemTemplate>
          <DataTemplate x:DataType="ui:FamilyInfoItem">

            <FlexLayout 
                Direction="Row"
                VerticalOptions="Fill">

              <Image 
                  Source="project_icon.png" 
                  WidthRequest="32" 
                  HeightRequest="32" 
                  Margin="2"/>

              <VerticalStackLayout
                  FlexLayout.Grow="1">

                <Label 
                    Text="{Binding FamilyName.Value}" 
                    FontSize="20" 
                    FontAttributes="Bold" 
                    Padding="2"/>

                <CollectionView 
                    ItemsSource="{Binding Persons}">

                  <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="ui:PersonInfoItem">
                      <Label 
                        Text="{Binding LastName.Value}" 
                        FontSize="12" 
                        Margin="10,0,0,0"
                        Padding="2"/>
                    </DataTemplate>
                  </CollectionView.ItemTemplate>
                
                </CollectionView>

              </VerticalStackLayout>

              <Button 
                  Text="❌" 
                  Clicked="OnDeleteFamilySelected"
                  WidthRequest="32" 
                  HeightRequest="32"
                  Padding="0"/>

            </FlexLayout>
          </DataTemplate>
        </CollectionView.ItemTemplate>
      </CollectionView>

    </VerticalStackLayout>

  </ScrollView>

</ContentPage>