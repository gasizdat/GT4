<?xml version="1.0" encoding="utf-8" ?>
<ContentView
    x:Class="GT4.UI.Components.DateInfoView"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:components="clr-namespace:GT4.UI.Components"
    x:Name="Root"
    x:DataType="components:DateInfoView">
  <Grid
      ColumnDefinitions="Auto, *"
      ColumnSpacing="20"
      RowDefinitions="Auto">

    <Label
        Grid.Row="0"
        Grid.Column="0"
        Style="{StaticResource FieldName}"
        Text="{Binding Caption, Source={x:Reference Root}}" />

    <Label
        Grid.Row="0"
        Grid.Column="1"
        Style="{StaticResource FieldValue}"
        Text="{Binding DateText, Source={x:Reference Root}}" />

  </Grid>
</ContentView>
